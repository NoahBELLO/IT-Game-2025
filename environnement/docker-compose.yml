services:
  frontend:
    container_name: frontend
    build:
      context: ../frontend
      dockerfile: Dockerfile
    restart: always
    ports:
      - 4200:4200
    volumes:
      - ../frontend:/usr/src/app  
    networks:
      - it_game_projet

networks:
  it_game_projet:
    driver: bridge
    external: true